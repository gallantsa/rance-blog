version: '3'

volumes:
  database_local:
  esdata_local:

services:
  rance-blog:
    build:
      context: .
      dockerfile: ./compose/local/Dockerfile
    image: rance-blog
    container_name: rance-blog
    working_dir: /app
    volumes:
      - database_local:/app/database
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - ES_URL=http://elasticsearch_local:9200
    command: /start.sh
    depends_on:
      - elasticsearch_local

  elasticsearch_local:
    build:
      context: .
      dockerfile: ./compose/production/elasticsearch/Dockerfile
    image: rance-blog_elasticsearch_local
    container_name: rance-blog_elasticsearch_local
    volumes:
      - esdata_local:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    environment:
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nproc: 65536
      nofile:
        soft: 65536
        hard: 65536
