# 阶段关键代码

> 记录时间：2026-06-20 | 阶段：Phase 0 — 项目初始化

## 1. pyproject.toml（依赖配置）

```toml
[project]
name = "rance-blog"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["django>=5.2,<6.0"]
```

- 使用 Django 5.2 LTS 系列
- Python 最低 3.14

## 2. manage.py（Django 入口）

由 `django-admin startproject config .` 自动生成，位于项目根目录。

```python
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()
```

关键点：
- 设置 `DJANGO_SETTINGS_MODULE=config.settings`，指向 `config/settings.py`
- 通过 `uv run python manage.py <command>` 执行

## 3. config/settings.py（核心配置节选）

```python
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
]

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

STATIC_URL = 'static/'
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
```

- 默认 SQLite 数据库
- BigAutoField 作为主键类型（支持更多数据量）
- 后续按需添加自定义 App

## 4. config/urls.py（根路由）

```python
from django.contrib import admin
from django.urls import path

urlpatterns = [
    path('admin/', admin.site.urls),
]
```

- 默认仅注册 Admin 路由
- 后续添加博客相关 URL 配置

## 5. 数据库初始化

```bash
python manage.py migrate
```

创建了以下数据表：
- `auth_user` / `auth_group` / `auth_permission` — 用户认证
- `django_admin_log` — Admin 操作日志
- `django_content_type` — 内容类型注册
- `django_session` — 会话管理

## 6. 创建并注册blog app

```bash
python manage.py startapp blog
```

在 `config/settings.py` 中添加：

```python
INSTALLED_APPS = [
    # ... 其他已安装应用 ...
    'blog',
]
```

## 7. blog/models.py（数据模型）

定义了 3 个模型：Category（分类）、Tag（标签）、Post（文章）。

### Category（分类）

```python
class Category(models.Model):
    name = models.CharField(max_length=100)
```

- 只有 `name` 一个字段，用作文章分类
- 与 Post 为一对多关系（ForeignKey）

### Tag（标签）

```python
class Tag(models.Model):
    name = models.CharField(max_length=100)
```

- 只有 `name` 一个字段，用作文章标签
- 与 Post 为多对多关系（ManyToManyField）

### Post（文章）

```python
class Post(models.Model):
    title = models.CharField(max_length=70)
    body = models.TextField()
    created_time = models.DateTimeField()
    modified_time = models.DateTimeField()
    excerpt = models.CharField(max_length=200, blank=True)
    category = models.ForeignKey(Category, on_delete=models.CASCADE)
    tags = models.ManyToManyField(Tag, blank=True)
    author = models.ForeignKey(User, on_delete=models.CASCADE)
```

| 字段 | 类型 | 说明 |
|------|------|------|
| title | CharField(70) | 文章标题 |
| body | TextField | 文章正文，大段文本使用 TextField |
| created_time | DateTimeField | 创建时间 |
| modified_time | DateTimeField | 最后修改时间 |
| excerpt | CharField(200, blank=True) | 文章摘要，允许为空 |
| category | ForeignKey → Category | 所属分类，级联删除 |
| tags | ManyToManyField → Tag | 关联标签，允许为空 |
| author | ForeignKey → User | 作者，关联 django auth.User，级联删除 |

## 8. 设置中文与上海时区（config/settings.py）

```python
# commit b589802
# 把英文改为中文
LANGUAGE_CODE = 'zh-hans'

# 把时区改为上海
TIME_ZONE = 'Asia/Shanghai'
```

- `LANGUAGE_CODE = 'zh-hans'` — 管理后台等界面显示中文
- `TIME_ZONE = 'Asia/Shanghai'` — 时间显示为北京时间（UTC+8）

## 9. 模型添加 `__str__` 方法（blog/models.py）

```python
# commit daabac5
class Category(models.Model):
    name = models.CharField(max_length=100)

    def __str__(self):
        return self.name


class Tag(models.Model):
    name = models.CharField(max_length=100)

    def __str__(self):
        return self.name


class Post(models.Model):
    # ... 字段定义 ...

    def __str__(self):
        return self.title
```

- `__str__` 是 Python 对象字符串表示方法
- Django Admin 后台和 shell 中通过此方法显示模型实例
- 各模型分别返回其有意义的字段：Category → name、Tag → name、Post → title

## 10. 生成数据库迁移文件

```bash
# commit daabac5
uv run python manage.py makemigrations
uv run python manage.py migrate
```

生成的迁移文件 `blog/migrations/0001_initial.py` 包含了三个模型的创建操作：
- `CreateModel(name='Category', ...)`
- `CreateModel(name='Tag', ...)`
- `CreateModel(name='Post', ...)` — 含 author（FK → User）、category（FK → Category）、tags（M2M → Tag）

执行 `migrate` 后在 SQLite 中创建了 4 张新表：
- `blog_category` — 分类表
- `blog_tag` — 标签表
- `blog_post` — 文章表
- `blog_post_tags` — 文章-标签多对多关联表

## 11. Django 首页视图与路由（"接客之道"）

### 配置模板目录（config/settings.py）

```python
import os  # 新增导入

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],  # 新增项目级模板目录
        # ...
    },
]
```

### 首页视图（blog/views.py）

```python
from django.http import HttpResponse
from django.shortcuts import render

def index(request):
    return render(request, 'blog/index.html', context={
        'title': '我的博客',
        'welcome': '欢迎来到我的博客首页'
    })
```

### 项目 URL 路由（config/urls.py）

```python
from django.contrib import admin
from django.urls import path, include  # 新增 include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('blog.urls')),  # 新增：根路径指向 blog 应用
]
```

### blog 应用 URL 路由（blog/urls.py）

```python
from django.urls import path
from . import views

urlpatterns = [
    path('', views.index, name='index'),
]
```

### 首页模板（templates/blog/index.html）

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}></title>
</head>
<body>
    <h1>{{ welcome }}</h1>
</body>
</html>
```

- 使用 Django 模板引擎渲染，传入 `title` 和 `welcome` 两个变量
- 页面路径：`http://127.0.0.1:8000/`

## 12. 首页集成 Bootstrap 与文章列表（commit 4f3a9ff）

### 静态资源

```
blog/static/blog/
├── css/
│   ├── bootstrap.min.css   # Bootstrap 3.3.2 框架
│   ├── custom.css          # 自定义样式（905 行）
│   └── pace.css            # 页面加载进度条
├── js/
│   ├── bootstrap.min.js
│   ├── jquery-2.1.3.min.js
│   ├── modernizr.custom.js
│   ├── pace.min.js
│   └── script.js
```

### 首页视图（blog/views.py）

```python
from django.shortcuts import render
from .models import Post

def index(request):
    post_list = Post.objects.all().order_by('-created_time')
    return render(request, 'blog/index.html', context={
        'post_list': post_list,
    })
```

- 从数据库查询所有 Post，按创建时间倒序排列
- 传入模板的 `post_list` 为 QuerySet 对象

### 首页模板变更

之前的极简模板替换为完整的文章列表页，包含：
- **文章卡片**：标题、分类、日期、作者、评论数、阅读数、摘要
- **分页占位符**：简单的页码导航
- **侧边栏**：最新文章、归档、分类、标签云、RSS
- **空状态**：`{% empty %}` 处理无文章时的 "暂时还没有发布的文章！"

## 13. 模型管理界面与字段中文化（commit c40bfcf）

### 模型 Meta 类与字段 verbose_name

```python
from django.utils import timezone

class Category(models.Model):
    name = models.CharField(max_length=100)

    class Meta:
        verbose_name = '分类'
        verbose_name_plural = verbose_name


class Tag(models.Model):
    name = models.CharField(max_length=100)

    class Meta:
        verbose_name = '标签'
        verbose_name_plural = verbose_name


class Post(models.Model):
    title = models.CharField("标题", max_length=70)
    body = models.TextField("正文")
    created_time = models.DateTimeField("创建时间", default=timezone.now)
    modified_time = models.DateTimeField("修改时间")
    excerpt = models.CharField("摘要", max_length=200, blank=True)
    category = models.ForeignKey(Category, verbose_name="分类", on_delete=models.CASCADE)
    tags = models.ManyToManyField(Tag, verbose_name="标签", blank=True)
    author = models.ForeignKey(User, verbose_name="作者", on_delete=models.CASCADE)

    def save(self, *args, **kwargs):
        self.modified_time = timezone.now()
        super().save(*args, **kwargs)

    class Meta:
        verbose_name = '文章'
        verbose_name_plural = verbose_name
```

| 变更 | 说明 |
|------|------|
| `verbose_name` | 每个 Model 添加 Meta 类，Admin 后台显示中文名 |
| 字段第一参数 | 全部改为中文名称字符串，如 `"标题"`、`"正文"` |
| `created_time` | 新增 `default=timezone.now`，创建时自动填充 |
| `save()` 方法 | 重写 `save()`，每次保存自动更新 `modified_time` |

### Admin 后台注册（blog/admin.py）

```python
from django.contrib import admin
from .models import Category, Tag, Post

class PostAdmin(admin.ModelAdmin):
    list_display = ['title', 'created_time', 'modified_time', 'category', 'author']
    fields = ['title', 'body', 'excerpt', 'category', 'tags']

    def save_model(self, request, obj, form, change):
        obj.author = request.user
        super().save_model(request, obj, form, change)

admin.site.register(Category)
admin.site.register(Tag)
admin.site.register(Post, PostAdmin)
```

- `list_display` — 列表页显示标题、时间、分类、作者等列
- `fields` — 编辑页显示的字段（排除 author，由 save_model 自动设置）
- `save_model` — 创建文章时自动将当前用户设为作者

### BlogConfig 配置（blog/apps.py）

```python
class BlogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'blog'
    verbose_name = '博客'
```

相应的 INSTALLED_APPS 改为：

```python
INSTALLED_APPS = [
    # ...
    'blog.apps.BlogConfig',  # 注册 blog 应用
]
```

## 14. 文章详情页与模板继承（commit 6348013）

### 模型添加 get_absolute_url

```python
from django.urls import reverse

class Post(models.Model):
    # ... 字段定义 ...

    def get_absolute_url(self):
        return reverse('blog:detail', kwargs={'pk': self.pk})
```

### blog/urls.py — 添加命名空间和详情路由

```python
app_name = 'blog'  # 新增应用命名空间

urlpatterns = [
    path('', views.index, name='index'),
    path('post/<int:pk>/', views.detail, name='detail'),  # 新增详情页路由
]
```

- `app_name = 'blog'` — URL 命名空间，用于模板中 `{% url 'blog:detail' post.pk %}`
- `path('post/<int:pk>/', ...)` — URL 参数，pk 为主键，类型为 int

### 详情视图（blog/views.py）

```python
from django.shortcuts import render, get_object_or_404
from .models import Post

def detail(request, pk):
    post = get_object_or_404(Post, pk=pk)
    return render(request, 'blog/detail.html', context={'post': post})
```

- `get_object_or_404` — 查询 Post，不存在时返回 404

### 基础模板（templates/base.html）

新建项目级 `templates/base.html`，包含：
- **导航栏**：首页、博客、关于、联系；搜索框
- **主内容区**：`{% block main %}` — 子模板填充
- **侧边栏**：`{% block toc %}` — 目录；固定组件（最新文章、归档、分类、标签云、RSS）
- **底部**：版权信息
- **移动端菜单**：Overlay 导航

### 文章详情模板（templates/blog/detail.html）

```html
{% extends 'base.html' %}

{% block main %}
  <article class="post post-{{ post.pk }}">
    <header class="entry-header">
      <h1 class="entry-title">{{ post.title }}</h1>
      <div class="entry-meta">
        <span class="post-category">{{ post.category.name }}</span>
        <span class="post-date">{{ post.created_time }}</span>
        <span class="post-author">{{ post.author }}</span>
      </div>
    </header>
    <div class="entry-content clearfix">
      {{ post.body }}
    </div>
  </article>
{% endblock main %}
```

### 首页模板重构

`templates/blog/index.html` 从独立完整页面重构为：

```html
{% extends 'base.html' %}

{% block main %}
  {% for post in post_list %}
    <article>
      ...
      <a href="{{ post.get_absolute_url }}">{{ post.title }}</a>
      ...
      <a href="{{ post.get_absolute_url }}" class="more-link">继续阅读 →</a>
      ...
    </article>
  {% empty %}
    <div class="no-post">暂时还没有发布的文章！</div>
  {% endfor %}
{% endblock main %}
```

- 继承 `base.html`，只填充 `{% block main %}`
- 文章标题和 "继续阅读" 链接改为指向 `post.get_absolute_url`（即 `/post/<pk>/`）

## 15. Markdown 支持与代码高亮（commit 5aae4e7）

### 依赖添加

```toml
# pyproject.toml
dependencies = [
    "django>=5.2,<6.0",
    "markdown>=3.10.2",
]
```

### 详情视图添加 Markdown 解析（blog/views.py）

```python
import markdown

def detail(request, pk):
    post = get_object_or_404(Post, pk=pk)
    post.body = markdown.markdown(post.body, extensions=[
        'markdown.extensions.extra',
        'markdown.extensions.codehilite',
        'markdown.extensions.toc',
    ])
    return render(request, 'blog/detail.html', context={'post': post})
```

### 代码高亮样式（templates/base.html）

```html
<!-- 引入 highlight.js 样式库 -->
<link href="https://cdn.bootcss.com/highlight.js/9.15.8/styles/github.min.css" rel="stylesheet">

<!-- highlight.js 行号样式 -->
<style>
.codehilite { padding: 0; }
.hljs-ln-numbers { user-select: none; text-align: center; color: #ccc; border-right: 1px solid #CCC; }
.hljs-ln-code { padding-left: 10px; white-space: pre; }
</style>

<!-- 引入 highlight.js 并启用行号 -->
<script src="https://cdn.bootcss.com/highlight.js/9.15.8/highlight.min.js"></script>
<script src="https://cdn.bootcss.com/highlightjs-line-numbers.js/2.7.0/highlightjs-line-numbers.min.js"></script>
<script>
    hljs.initHighlightingOnLoad();
    hljs.initLineNumbersOnLoad();
</script>
```

### 详情模板转义控制

```html
<!-- detail.html：之前 -->
{{ post.body }}

<!-- detail.html：改为 safe 过滤器，让 Markdown 渲染的 HTML 生效 -->
{{ post.body | safe }}
```

## 16. 文章目录支持（commit 85aeea9）

### 修复 markdown.markdown → markdown.Markdown（bug 修复）

```python
import re
from django.utils.text import slugify
from markdown.extensions.toc import TocExtension

def detail(request, pk):
    post = get_object_or_404(Post, pk=pk)
    md = markdown.Markdown(extensions=[
        'markdown.extensions.extra',
        'markdown.extensions.codehilite',
        TocExtension(slugify=slugify),
    ])
    post.body = md.convert(post.body)

    # 提取目录结构
    m = re.search(r'<div class="toc">\s*<ul>(.*)</ul>\s*</div>', md.toc, re.S)
    post.toc = m.group(1) if m is not None else ''

    return render(request, 'blog/detail.html', context={'post': post})
```

`md.toc` 由 `TocExtension` 注入到 Markdown 实例中，包含完整的 HTML 目录。

### 详情模板目录块

```html
{% block toc %}
  {% if post.toc %}
    <div class="widget widget-content">
      <h3 class="widget-title">文章目录</h3>
      <div class="toc">
        <ul>
          {{ post.toc|safe }}  <!-- 目录内容，需要 safe 过滤器 -->
        </ul>
      </div>
    </div>
  {% endif %}
{% endblock toc %}
```

## 17. 摘要自动生成（commit 0dbb1c1）

### Post 模型的 save 方法增强（blog/models.py）

```python
import markdown
from django.utils.html import strip_tags

class Post(models.Model):
    # ... 字段定义 ...

    def save(self, *args, **kwargs):
        self.modified_time = timezone.now()

        # 实例化 Markdown，去掉 TocExtension（摘要不需要目录）
        md = markdown.Markdown(extensions=[
            'markdown.extensions.extra',
            'markdown.extensions.codehilite',
        ])

        # Markdown → HTML → 去除标签 → 取前 54 个字符作为摘要
        self.excerpt = strip_tags(md.convert(self.body))[:54]

        super().save(*args, **kwargs)
```

**流程**：`body (Markdown)` → `md.convert()` → `HTML` → `strip_tags()` → `纯文本[:54]` → `excerpt`

### 首页模板

```html
<!-- index.html -->
<p>{{ post.excerpt }}...</p>
```

- 自动生成的摘要末尾追加 `...` 省略号

## 18. 自定义模板标签与侧边栏小部件（commit 7f71003）

### 目录结构

```
blog/templatetags/
├── __init__.py
└── blog_extras.py          # 自定义模板标签

templates/blog/inclusions/
├── _recent_posts.html      # 最新文章小部件
├── _archives.html          # 归档小部件
├── _categories.html        # 分类小部件
└── _tags.html              # 标签云小部件
```

### 注册模板标签库（blog/templatetags/blog_extras.py）

```python
from django import template
from ..models import Post, Category, Tag

register = template.Library()


@register.inclusion_tag('blog/inclusions/_recent_posts.html', takes_context=True)
def show_recent_posts(context, num=5):
    return {
        'recent_post_list': Post.objects.all().order_by('-created_time')[:num],
    }


@register.inclusion_tag('blog/inclusions/_archives.html', takes_context=True)
def show_archives(context):
    return {
        'date_list': Post.objects.dates('created_time', 'month', order='DESC'),
    }


@register.inclusion_tag('blog/inclusions/_categories.html', takes_context=True)
def show_categories(context):
    return {
        'category_list': Category.objects.all(),
    }


@register.inclusion_tag('blog/inclusions/_tags.html', takes_context=True)
def show_tags(context):
    return {
        'tag_list': Tag.objects.all(),
    }
```

| 标签 | 功能 | 数据来源 |
|------|------|---------|
| `{% show_recent_posts %}` | 最新文章（默认 5 篇） | `Post.objects.all()[:5]` |
| `{% show_archives %}` | 按月归档 | `Post.objects.dates('created_time', 'month')` |
| `{% show_categories %}` | 全部分类 | `Category.objects.all()` |
| `{% show_tags %}` | 全部标签 | `Tag.objects.all()` |

### base.html 使用

```html
{% load static %}
{% load blog_extras %}     <!-- 加载自定义标签库 -->

<!-- 侧边栏中替换硬编码 HTML -->
{% show_recent_posts %}
{% show_archives %}
{% show_categories %}
{% show_tags %}
```

原来侧边栏中的硬编码 HTML 被移除，改为动态渲染的 inclusion tag。

## 19. 归档、分类、标签视图（commit 70b0283）

### 新增 URL 路由（blog/urls.py）

```python
app_name = 'blog'
urlpatterns = [
    path('', views.index, name='index'),
    path('post/<int:pk>/', views.detail, name='detail'),
    path('archives/<int:year>/<int:month>/', views.archive, name='archive'),  # 新增
    path('categories/<int:pk>/', views.category, name='category'),            # 新增
    path('tags/<int:pk>/', views.tag, name='tag'),                            # 新增
]
```

### 新增视图（blog/views.py）

```python
from .models import Post, Category, Tag   # 补充导入 Category 和 Tag


def archive(request, year, month):
    """按年月归档：过滤指定年月的文章"""
    post_list = Post.objects.filter(
        created_time__year=year,
        created_time__month=month
    ).order_by('-created_time')
    return render(request, 'blog/index.html', context={'post_list': post_list})


def category(request, pk):
    """按分类筛选：过滤指定分类的文章"""
    cate = get_object_or_404(Category, pk=pk)
    post_list = Post.objects.filter(category=cate).order_by('-created_time')
    return render(request, 'blog/index.html', context={'post_list': post_list})


def tag(request, pk):
    """按标签筛选：过滤指定标签的文章"""
    t = get_object_or_404(Tag, pk=pk)
    post_list = Post.objects.filter(tags=t).order_by('-created_time')
    return render(request, 'blog/index.html', context={'post_list': post_list})
```

| 视图 | URL | 筛选条件 | ORM 查询 |
|------|-----|---------|---------|
| `archive` | `/archives/<year>/<month>/` | 年月 | `created_time__year` / `__month` |
| `category` | `/categories/<pk>/` | 分类 ID | `category=cate` |
| `tag` | `/tags/<pk>/` | 标签 ID | `tags=t` |

三个视图均复用 `blog/index.html` 模板，通过 `post_list` 上下文变量渲染筛选结果。

### 侧边栏链接动态化

三个 inclusion 子模板中的 `#` 占位链接改为真实的 URL：

```html
<!-- _archives.html -->
<a href="{% url 'blog:archive' date.year date.month %}">

<!-- _categories.html -->
<a href="{% url 'blog:category' category.pk %}">

<!-- _tags.html -->
<a href="{% url 'blog:tag' tag.pk %}">
```
