version: '3'

volumes:
  static:
  database:
  esdata:

services:
  rance-blog:
    build:
      context: .
      dockerfile: compose/production/django/Dockerfile
    image: rance-blog
    container_name: rance-blog
    working_dir: /app
    volumes:
      - database:/app/database
      - static:/app/static
    env_file:
      - .envs/.production
    ports:
      - "8000:8000"
    command: /start.sh
    depends_on:
      - elasticsearch

  rance-blog-nginx:
    build:
      context: .
      dockerfile: compose/production/nginx/Dockerfile
    image: rance-blog-nginx
    container_name: rance-blog-nginx
    depends_on:
      - rance-blog
    volumes:
      - static:/app/static
    ports:
      - "80:80"
      - "443:443"

  elasticsearch:
    build:
      context: .
      dockerfile: ./compose/production/elasticsearch/Dockerfile
    image: rance-blog_elasticsearch
    container_name: rance-blog_elasticsearch
    volumes:
      - esdata:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    environment:
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nproc: 65536
      nofile:
        soft: 65536
        hard: 65536
